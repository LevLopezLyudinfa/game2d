package Model;
public interface ImplementMovement {
    static void startPositionx(){
       int x = (int) (Math.random()*32);
    }
    static void startPositiony(){
       int y = (int) (Math.random()*32);
    }
}
